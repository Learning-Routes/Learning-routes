<% content_for(:title, "Learning Routes - AI-Powered Personalized Learning") %>

<div class="w-full max-w-4xl mx-auto text-center">
  <div class="py-16 sm:py-24">
    <h1 class="text-5xl sm:text-6xl font-bold text-gray-900 tracking-tight">
      Learn anything with
      <span class="text-indigo-600">AI-powered</span>
      routes
    </h1>
    <p class="mt-6 text-lg text-gray-600 max-w-2xl mx-auto">
      Personalized learning paths generated by AI, adapted to your level and learning style.
      Master any topic with procedural routes that evolve with you.
    </p>
    <div class="mt-10 flex items-center justify-center gap-4">
      <%= link_to "Get started", core.sign_up_path,
          class: "rounded-lg bg-indigo-600 px-8 py-3.5 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 transition" %>
      <%= link_to "Sign in", core.sign_in_path,
          class: "rounded-lg border border-gray-300 px-8 py-3.5 text-sm font-semibold text-gray-700 hover:bg-gray-50 transition" %>
    </div>
  </div>

  <!-- Feature cards -->
  <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 mt-8 mb-16">
    <% [
      ["🤖", "AI-Generated Routes", "Personalized learning paths created by advanced AI models"],
      ["📊", "Adaptive Difficulty", "Content adjusts based on your performance and progress"],
      ["🎯", "Knowledge Gap Analysis", "Identifies weak spots and generates reinforcement routes"]
    ].each do |emoji, title, description| %>
      <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-8 text-center">
        <span class="text-4xl"><%= emoji %></span>
        <h3 class="mt-4 text-lg font-semibold text-gray-900"><%= title %></h3>
        <p class="mt-2 text-sm text-gray-500"><%= description %></p>
      </div>
    <% end %>
  </div>
</div>
